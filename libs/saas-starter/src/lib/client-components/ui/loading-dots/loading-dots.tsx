import s from './locading-dots.module.css';

const LoadingDots = () => {
  return (
    <span className={s.root}>
      <span />
      <span />
      <span />
    </span>
  );
};

export default LoadingDots;
